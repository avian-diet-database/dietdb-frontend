import { useState } from "react"
import { DesignHeader } from "../design/DesignHeader";

interface LogicHeaderProps {
}

export const LogicHeader = (props: LogicHeaderProps) => {

    return DesignHeader();
}