import { useState } from "react"
import { DesignHomeButton } from "../design/DesignHomeButton";

interface LogicHomeButtonProps {
}

export const LogicHomeButton = (props: LogicHomeButtonProps) => {

    return DesignHomeButton();
}